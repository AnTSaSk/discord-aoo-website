services:
  website:
    image: ghcr.io/${GITHUB_REPOSITORY}:${IMAGE_TAG:-latest}
    networks:
      - traefik-public
    deploy:
      replicas: 1
      placement:
        constraints:
          - node.role == manager
      update_config:
        parallelism: 1
        delay: 10s
        failure_action: rollback
        order: start-first
      rollback_config:
        parallelism: 1
        delay: 10s
      restart_policy:
        condition: on-failure
        delay: 5s
        max_attempts: 3
        window: 120s
      resources:
        limits:
          memory: 64M
          cpus: "0.15"
        reservations:
          memory: 32M
          cpus: "0.05"
      labels:
        - "traefik.enable=true"
        - "traefik.http.routers.discord-aoo-website.rule=Host(`${APP_DOMAIN:-albion-online-objectives.com}`)"
        - "traefik.http.routers.discord-aoo-website.entrypoints=websecure"
        - "traefik.http.routers.discord-aoo-website.tls.certresolver=letsencrypt"
        - "traefik.http.services.discord-aoo-website.loadbalancer.server.port=8080"
        - "traefik.http.routers.discord-aoo-website.middlewares=aoo-ratelimit"
        - "traefik.http.middlewares.aoo-ratelimit.ratelimit.average=100"
        - "traefik.http.middlewares.aoo-ratelimit.ratelimit.burst=50"
        - "traefik.http.middlewares.aoo-ratelimit.ratelimit.period=1s"
    # NOTE: security_opt is not supported by 'docker stack deploy' in Swarm mode —
    # no-new-privileges is enforced at Docker daemon level (daemon.json).
    cap_drop:
      - ALL
    cap_add:
      - SETGID
      - SETUID
      - NET_BIND_SERVICE
    read_only: true
    volumes:
      # NOTE: service-level 'tmpfs:' key is silently ignored by 'docker stack deploy'.
      # Use 'type: tmpfs' in volumes instead (maps to --mount type=tmpfs).
      # /tmp has mode 1777 in image → tmpfs inherits 1777 → UID 101 can write.
      # nginx.conf redirects all writable paths (cache, logs, pid) to /tmp.
      - type: tmpfs
        target: /tmp
        tmpfs:
          size: 33554432 # 32MB
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://127.0.0.1:8080/"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s

networks:
  traefik-public:
    external: true

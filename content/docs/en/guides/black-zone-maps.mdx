---
title: Black Zone Maps — Complete List
description: Complete list of 276 Black Zone maps in Albion Online, organized by region with 6 Wilderness Boss territories. Find any map for the /add command.
navIcon: 🗺️
---

import MapBrowser from '@/components/docs/MapBrowser';

Albion Online Objectives (AOO) supports **276 Black Zone maps** for the [`/add`](/docs/commands/add/) command. The `map` parameter uses Discord autocomplete: start typing any part of the map name (beginning, middle, or end) and matching results will appear.

<MapBrowser client:visible />

## Wilderness Boss Maps

These 6 maps contain Wilderness Bosses, powerful open-world encounters:

| Map |
|-----|
| Black Monastery |
| Citadel of Ash |
| Eldersleep |
| Eye of the Forest |
| Unhallowed Cloister |
| Wailing Bulwark |

## Using Map Autocomplete

When using the [`/add`](/docs/commands/add/) command, start typing any part of the map name. Type `Murder` to find Murdergulch maps, `Cross` for maps containing "Cross" (Dryvein Cross, Everwinter Crossing, etc.), or `Ravine` to find all maps ending in Ravine.

The autocomplete will show up to 25 matching results at a time.

## Related

- The [objective types reference](/docs/guides/objective-types/) covers all types and rarities
- The [`/add` command reference](/docs/commands/add/) documents the command in full
